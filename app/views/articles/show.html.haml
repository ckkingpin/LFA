.row
  %h1=@article.title
  %p=@article.content.html_safe

  -unless @article.categories.blank?
    %label Categories
    = render 'shared/categories_list', article: @article

  = text_area_tag 'article_content', @article.content
  %pre{id:'editor-preview'}

  = link_to "Back", root_path, class: 'btn btn-primary'
  - if can? :manage, @article
    = link_to "Edit", edit_article_path(@article), class: 'btn btn-success'
    = link_to "Delete", article_path(@article), method: :delete, data: { confirm: 'Are You Sure You Want To Delete?'}, class: 'btn btn-danger'
