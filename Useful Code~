Useful Command Lines

git init > initialises project to git
git add . > adds current project to respirotory
git status > provides current status of files on respirotory
git commit -m "Initial commit" > saves all unsaved files to the respirotory
git log > shows all commit logs for the particular project

git push > pushes to the servers

git merge upstream/master

git fetch upstream
git checkout master

CHECKING HOW MANY USERS ARE REGISTERED

rails c > brings up console
User.connection > makes a connection to the db
User.count > provides you the number of users registered

rake routes > shows all routes on db

